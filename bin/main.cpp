#include "../src/db/MyCoolDB.h"

int main() {
//  auto req =
//                                 "\t CREATE TABLE table_name (id INT PRIMARY KEY); "
//                                 "SELECT * FROM table_name; "
//                                 "INSERT INTO table_name VALUES (id1), (id2);"
//                                 "SELECT * FROM table_name WHERE id = 1; ";
//
//  MyCoolDB db;
//  db.ExecuteCommand("CREATE TABLE table_name (id INT PRIMARY KEY, name VARCHAR NOT NULL);");
//  db.ExecuteCommand("INSERT INTO table_name VALUES (1, 'toy'), (1, 'phone');");
//  auto e = db.ExecuteCommand("CREATE TABLE table_name (id INT PRIMARY KEY, name VARCHAR NOT NULL);");

  MyCoolDB db;
  //db.Open("...");

  //std::string json_request = std::string("SELECT json FROM data WHERE id = ") + std::to_string(1) + ";";
  //auto city = db.ExecuteCommand(json_request.c_str());

  return 0;
}